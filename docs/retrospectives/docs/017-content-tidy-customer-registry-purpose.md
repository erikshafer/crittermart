---
retrospective: 017
kind: docs
prompt: docs/prompts/docs/017-content-tidy-customer-registry-purpose.md
deliverable: openspec/specs/customer-registry/spec.md (real ## Purpose over the archive-stamped TBD), docs/prompts/README.md + docs/retrospectives/README.md (docs count 16→17)
date: 2026-07-17
mode: solo
session-runner: Claude (Opus 4.8)
---

# Retrospective — Docs 017: Content-Tidy — write the real `customer-registry` `## Purpose`

## Outcome summary

A **voluntary content-tidy** the owner chose from the post-Promotions open next-direction fork: drain the second (and last) archive-stamped TBD `## Purpose` in the OpenSpec workspace, giving the `customer-registry` capability the real Purpose the `customer-registry` archive left as a placeholder. Unlike docs/016 (a *cadence-owed* design-return that also ran a CLI archive), this session is **not owed** — the design-return counter was already reset by PR #147, and the workspace was already at 0 active changes with `customer-registry` synced into `openspec/specs/`. The ceremony level here is set by the **tidy-ceremony rule** alone (a tidy that authors spec content carries the full pair), not by cadence.

- **`customer-registry` `## Purpose`** — replaced the archive-stamped *"TBD - created by archiving change customer-registry. Update Purpose after archive."* with a real, sibling-caliber paragraph: the Identity BC's one **non-event-sourced** shape (EF Core + Npgsql; the `Customer` row *is* the read model; `CustomerRegistered` an outbox notification, not a stream), `RegisterCustomer` (`POST /customers`, server-minted or explicit id) with the normalized-email double-guard (app-layer `409` + DB unique index), the **Open-Host Service** `GET /customers/{id}` read + the **Published Language** `CustomerRegistered` event that Orders folds into `LocalCustomerView` (slice 5.4, no sync HTTP per ADR 001), the `EmailChange` **EF-Core-backed convention saga** (open/confirm/timeout, deadline-scheduled-once because Wolverine has no scheduled-message cancellation), and the **ADR 023** auth-issuer story (ASP.NET Core Identity, self-signed asymmetric JWT, offline verification against the config-distributed public key, `sub` as the **sole** trust boundary with `X-Customer-Id` fully retired, client-side-discard logout). Closes with the capability's place — the Identity BC's sole capability — and the 5.1–5.11 slice→provenance map.
- **Index READMEs** — `docs/` count 16 → 17 in both `docs/prompts/README.md` and `docs/retrospectives/README.md`, population note extended with this content-tidy.

**Verification**: `openspec validate customer-registry --specs --strict` → 6 passed / 0 failed (prose-only edit; the eight requirements are untouched). `openspec list` → **No active changes found** — the workspace baseline is preserved. No code, no tests, no live boot (a docs/spec tidy).

## What worked

- **The retro docs/016 methodology note pre-named this exact session.** Docs/016 recorded the "archive-stamped TBD Purpose is a recurring debt the archive CLI creates" refinement *and* explicitly flagged `customer-registry` as "the next instance awaiting the same treatment." This session is the refinement paying off: the pattern was already established, so the work was mechanical to scope and the only real authoring effort was the Purpose prose itself.
- **Writing the Purpose from the shipped eight-requirement set kept it accurate.** With every requirement in front of me, the Purpose could name the real endpoints (`POST /customers`, `POST /register`, `POST /login`, `GET /customers/{id}`), the real saga store (EF-Core, not Marten), and the real auth contract (`sub`, offline verification, config-distributed public key) — precisely, not from a modeling-time approximation.
- **Framing the "why full pair" honestly mattered.** It would have been easy to copy docs/016's "design-return" framing, but this session is *not* cadence-owed (the counter is reset). Naming the ceremony driver correctly — the tidy-ceremony rule, not the design-return cadence — keeps the methodology record from mislabeling a voluntary tidy as an owed interleave. The distinction is small but it is exactly the kind of drift the retro layer exists to catch.

## What was harder / notable

- **A third latent doc-staleness surfaced, one layer up from the spec: the context-map auth line.** `docs/context-map/README.md` § *Round-one stubs and deferrals* still narrates the auth cutover as in-progress — *"Until the auth slices (§§ 5.8–5.11) ship, the `X-Customer-Id` seam still carries a hardcoded id…once they ship, the JWT's `sub` claim replaces it."* Those slices **shipped**, and the `X-Customer-Id` hard cutover retired the fallback entirely (the `customer-registry` spec's *Verify a token* requirement is explicit: the header "no longer resolves identity"). So the context-map prose reads a step behind reality. Left untouched this session (out of scope — no opportunistic edits, and the prompt named the exclusion); recorded below as a ready doc-tidy candidate.
- **`openspec validate <capability> --specs` validates the whole workspace, not just the named capability.** The command reported all six specs, not `customer-registry` alone. Harmless (all passed, and the target line is clearly present), but worth noting so a future session doesn't read the six-spec output as a scoping error.

## Methodology refinements

- **The workspace's archive-stamped-TBD debt is now fully drained — a closable class, not a standing chore.** Two capabilities were born by archiving a change (`coupon-promotion`, docs/016; `customer-registry`, this session), and both TBDs are now real Purposes. Any *future* archive-born capability will seed a fresh TBD, but the two round-one instances are closed. The general rule holds: **write the real Purpose in the tidy that has the capability's full requirement set visible** — for an archive-born capability, that is the design-return archiving its storefront-completing slice (docs/016) or a later voluntary content-tidy once the set is stable (this session).
- **A voluntary content-tidy is a legitimate next-direction pick, not only a cadence obligation.** Docs/016 ran because the cadence *owed* an interleave; this one ran because the owner *chose* it from an open fork with the counter already reset. Both are valid reasons to spend a `tidy:` PR authoring spec content — the tidy-ceremony rule governs the *shape* (full pair) regardless of *why* the session was picked. Recording this keeps future sessions from assuming every Purpose-write must be cadence-justified.

## Outstanding / next-session inputs

- **Cadence unchanged — the counter stays clear.** This voluntary tidy neither owes nor discharges a design-return (the counter was already reset by #147). The **next PR remains an open pick** — the standout build direction is still the second DCB (`one-redemption-per-customer`, Workshop 003 §8 item 6 / ADR 024's composite `(coupon × customer)` tag), with the shared-discount-budget and coupon-lifecycle variants as siblings. The OpenSpec workspace is at **0 active changes** — a clean baseline for the next proposal (name it **without** a date prefix; the archive CLI stamps the date).
- **Context-map auth-cutover staleness** — a ready doc-tidy candidate: `docs/context-map/README.md` § *Round-one stubs* still describes the `X-Customer-Id` seam as live pending the auth slices, but the hard cutover retired it. A small, self-contained prose reconciliation, natural as a rider on the next design-return or a standalone light tidy. (This is the *third* latent doc-staleness the Promotions-era tidies have surfaced — first the two TBD Purposes, now this — suggesting a broader "post-cutover doc sweep" could be worth a single consolidating session.)
- **Deferred from #146 (unchanged, non-blocking):** the **visual W2/W3 browser-verify** — slice 6.2's live-verify was API-level only; the Claude-in-Chrome extension was unconnected, so the SPA drive + GIF are unproven in a real browser (UI rendering is covered by the 123 client Vitest tests). To close: connect the extension, boot the stack (demo-runbook Step 1 + 5d), drive `http://localhost:5273`.
- **Carry-forwards (unchanged, non-blocking):** two remote branches await delete/keep (`origin/feat/cart-identity-harmonization`, `origin/research/cw-telemetry-spike`); dependabot #132–139 re-triage; `UseDurableLocalQueues()` saga-timeout + `ReplenishTimeout` verification gaps; refresh/revocation (ADR 023 Q15) + authZ/roles (Q16) deferred. **POST-TALK:** delete the AppHost demo knobs (payment decline/delay, order timeout, replenish timeout). **Locked/standing:** Wolverine stays **6.19.0** ([[critterwatch-wolverine-version-coupling]]); Marten **9.15.1**; CritterWatch trial **expired 2026-07-10** (console blocked); advisory-is-advisory (the coupon validate query never guards checkout); frontend units run with `--exclude "**/e2e/**"`.

## Spec-delta — landed?

**Named delta landed.** The prompt named a single spec-content edit: the `customer-registry` capability's real **`## Purpose`** over the archive-stamped TBD, with **no requirement or scenario change**. It landed: `openspec/specs/customer-registry/spec.md` now opens with a real, sibling-caliber Purpose; `openspec validate customer-registry --specs --strict` confirms the spec still valid (the eight requirements untouched); `openspec list` confirms the workspace unchanged at **No active changes**. This is a **voluntary content-tidy** closing the second archive-stamped-TBD instance — four-step closure: **prompt named → session executed → this retro confirms → the main spec recorded** (the Purpose prose is itself the record; there is no `## Document History` on OpenSpec specs, so the retro + the spec text carry the closure). This tidy authored spec content (the Purpose), so it carried the full prompt/retro pair per the tidy-ceremony rule — driven by that rule alone, not by the design-return cadence (which remains reset).
