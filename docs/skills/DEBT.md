# Skills Debt

Skill-file gaps surfaced during sessions but deferred to a future `tidy: skills` PR (per CLAUDE.md § One prompt = one session = one PR: surfaced-but-not-blocking gaps land here, not in the surfacing session's PR).

| # | Surfaced by | Candidate skill | What it would capture | Status |
|---|---|---|---|---|
| 1 | Retros 008, 011, 012; **third use landed in implementations/013 (slice 3.4)** | `marten-projection-conventions` (local) | The three CritterMart Marten projection conventions, each now used 3×: **(a) instance-registered projections** carrying config (`OrdersAwaitingPaymentProjection`, `CartsAwaitingActivityProjection` — both take the configured timeout so the visible deadline matches the schedule); **(b) `IEvent<T>` metadata folds** (`OrdersAwaitingPayment`, `CartsAwaitingActivity`, `CartView.LastActivityAt` — wrap the event to read its append timestamp; unit tests construct `new Event<T>(data) { Timestamp = … }`); **(c) conditional deletes** via the `ShouldDelete(Event)` method convention (both todo-list projections, deleting on terminal events). Plus the hard-won nuance from 013: **convention-method projections MUST be `partial`** (Marten 9's compile-time source generator has no runtime fallback; the host refuses to boot otherwise) — and `Identity<IEvent<T>>(…)` as the multi-stream analog of the metadata fold. Defer-to-upstream check: the upstream `marten-projections-single-stream` / `marten-projections-multi-stream` skills cover the APIs; the *local* skill would capture only the CritterMart conventions (instance registration for config injection, the deadline-mirroring idiom, the test-construction pattern). | **Drained** — [`marten-projection-conventions/SKILL.md`](marten-projection-conventions/SKILL.md), authored in the `tidy: encode` bundle (prompt rules/002) |
