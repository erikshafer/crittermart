# Skills Debt

Skill-file gaps surfaced during sessions but deferred to a future `tidy: skills` PR (per CLAUDE.md § One prompt = one session = one PR: surfaced-but-not-blocking gaps land here, not in the surfacing session's PR).

| # | Surfaced by | Candidate skill | What it would capture | Status |
|---|---|---|---|---|
| 1 | Retros 008, 011, 012; **third use landed in implementations/013 (slice 3.4)** | `marten-projection-conventions` (local) | The three CritterMart Marten projection conventions, each now used 3×: **(a) instance-registered projections** carrying config (`OrdersAwaitingPaymentProjection`, `CartsAwaitingActivityProjection` — both take the configured timeout so the visible deadline matches the schedule); **(b) `IEvent<T>` metadata folds** (`OrdersAwaitingPayment`, `CartsAwaitingActivity`, `CartView.LastActivityAt` — wrap the event to read its append timestamp; unit tests construct `new Event<T>(data) { Timestamp = … }`); **(c) conditional deletes** via the `ShouldDelete(Event)` method convention (both todo-list projections, deleting on terminal events). Plus the hard-won nuance from 013: **convention-method projections MUST be `partial`** (Marten 9's compile-time source generator has no runtime fallback; the host refuses to boot otherwise) — and `Identity<IEvent<T>>(…)` as the multi-stream analog of the metadata fold. Defer-to-upstream check: the upstream `marten-projections-single-stream` / `marten-projections-multi-stream` skills cover the APIs; the *local* skill would capture only the CritterMart conventions (instance registration for config injection, the deadline-mirroring idiom, the test-construction pattern). | **Drained** — [`marten-projection-conventions/SKILL.md`](marten-projection-conventions/SKILL.md), authored in the `tidy: encode` bundle (prompt rules/002) |
| 2 | This session — CritterWatch console boot failure; fixed in PR #55 | `critterwatch-install` (**global/upstream**, not a local skill) | The **inverse shared-queue DLQ collision**. The skill's pitfall table has a row for over-broad `DisableDeadLetterQueueing()` killing the *services'* DLQs, but not the opposite hazard: when the **console** disables DLQ on the shared `critterwatch` queue while the **monitored services** declare that same queue with Wolverine's default `x-dead-letter-exchange`, RabbitMQ rejects the second declaration with `PRECONDITION_FAILED` (406, "inequivalent arg 'x-dead-letter-exchange'") and whichever side starts *second* crashes. It is **order-dependent / latent**: `.WaitFor(console)` masks it until startup order inverts (a standalone single-service run, a race, a prior crashed boot) or a stale `with`-DLX queue survives on a long-lived broker container across restarts. The missing guidance: the `critterwatch` and `{service}-control` queues are **shared declarations that must carry identical args on every app that touches them** — simplest is to NOT disable DLQ on the console so it matches the services' default (the harmless `wolverine-dead-letter-queue` is cheaper than a queue-arg collision). Wants a new pitfall row + a one-line "shared queues need consistent declaration args" note. | **Deferred — upstream.** `critterwatch-install` lives in `~/.claude/skills/` (global), so the fix belongs in the **`ai-skills` repo**, not CritterMart's local `docs/skills/`. CritterMart-side fix already shipped in PR #55. Take the pitfall-row discussion to `ai-skills`. |
| 3 | Workshop 003 session (PR #142) — ADR 024's assembly verification + a post-merge skills glance | `marten-advanced-dynamic-consistency-boundary` + `marten-advanced-cross-stream-operations` (**global/upstream**, not local skills) | **DCB is no longer Polecat-only, and the skills say it is.** The DCB skill's entire API surface is Polecat-flavored (`[BoundaryModel]`, `Load() => EventTagQuery.For(…).AndEventsOfType<…>()`, `IEventBoundary<T>`) with an explicit "DCB is currently implemented in Polecat" note; the cross-stream skill's DCB comparison table says "Availability: **Polecat only**." Both are stale against Marten 9.11.0, where DCB is first-class — verified in the pinned `Marten.dll` (ADR 024): `FetchForWritingByTags`, `EventTagQuery().Or<CouponId>(id)`, `DcbConcurrencyException`, opt-in schema (`tags TEXT[]` + GIN index on `mt_events`). Wants: an availability correction in both skills plus the **Marten** API surface documented alongside (or instead of) the Polecat one. Risk while unfixed: an implementation session pattern-matches the skill and copies Polecat symbols into a Marten codebase — Workshop 003 §§ 4/8 fence this for the Promotions slices, but the fence is per-project; the fix belongs upstream. | **Deferred — upstream.** Both skills live in `~/.claude/skills/` (global), so the correction belongs in the **`ai-skills` repo**, not CritterMart's local `docs/skills/`. CritterMart-side fencing already shipped in ADR 024 + Workshop 003 (PR #131/#142). Take the availability-row + Marten-API discussion to `ai-skills`. |
